import { RouteIndex } from './RouteIndex';

const routeIndex = {
  path: '/teams',
  element: <RouteIndex/>,
}

const linkIndex = {
  path: '/teams',
  label: 'teams'
}

export { routeIndex, linkIndex };
