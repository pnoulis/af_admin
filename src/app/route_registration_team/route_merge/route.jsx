import { RouteMerge } from "./RouteMerge";

const routeMerge = {
  path: "/registration/team/merge",
  element: <RouteMerge />,
};

const linkMerge = {
  path: "/registration/team/merge",
  label: "merge",
};

export { routeMerge, linkMerge };
