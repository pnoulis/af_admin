import { RouteIndex } from "./RouteIndex";

const routeIndex = {
  path: "/registration/team",
  element: <RouteIndex />,
};

const linkIndex = {
  path: "/registration/team",
  label: "registration",
};

export { routeIndex, linkIndex, RouteIndex };
