import { RouteSummary } from "./RouteSummary";

const routeSummary = {
  path: "/registration/team/summary",
  element: <RouteSummary />,
};

const linkSummary = {
  path: "/registration/team/summary",
  label: "summary",
};

export { routeSummary, linkSummary, RouteSummary };
