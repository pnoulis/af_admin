import { RoutePlayers } from "./RoutePlayers";

const routePlayers = {
  path: "/registration/team/players",
  element: <RoutePlayers />,
};

const linkPlayers = {
  path: "/registration/team/players",
  label: "add players",
};

export { routePlayers, linkPlayers, RoutePlayers };
